


// Instantiate the module
regfile instance_name (
    .clk(clk), 
    .rst(rst), 
    .raddr1(raddr1), 
    .raddr2(raddr2), 
    .waddr(waddr), 
    .wdata(wdata), 
    .w_en(w_en), 
    .rdata1(rdata1), 
    .rdata2(rdata2)
    );


